# agent-setup configuration — sourced by scripts/sync.sh
# Windows username (used only when running under WSL to sync the Windows side).
WINDOWS_USER="Alex Jordan"

# All skills in skills/ sync to ~/.agents/skills (cross-tool standard dir).
# CLAUDE.md / AGENTS.md at the repo root sync to ~/.claude/CLAUDE.md and
# ~/.agents/AGENTS.md (old target backed up to <path>.bak-<timestamp> before overwrite).
