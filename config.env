# agent-setup configuration — sourced by scripts/sync.sh
# Windows username (used only when running under WSL to sync the Windows side).
WINDOWS_USER="Alex Jordan"

# Skills additionally synced into ~/.codex/skills (Codex-specific set).
# Everything in skills/ always goes to ~/.agents/skills.
CODEX_SKILLS="orchestrator use-loop explain-diff simplify self-test autoreview chrome-cdp"
